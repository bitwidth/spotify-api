# Spotify Backend (SAM + Lambda + Vue via Vercel)

- Connect Spotify via Authorization Code flow (login + callback)
- List the artists the user follows
- Stop current playback
- Start playing one of the user's top 10 tracks

Env:
- SPOTIFY_CLIENT_ID
- SPOTIFY_CLIENT_SECRET
- SPOTIFY_REDIRECT_URI
- TOKENS_TABLE (SAM)